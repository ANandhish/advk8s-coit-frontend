apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: frontend-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - "frontend.nanik8s.xyz"
    secretName: frontend.nanik8s.xyz
  rules:
  - host: frontend.nanik8s.xyz
    http:
      paths:
      - backend:
          service:
            name: coit-frontend-lb
            port:
              number: 80
        path: /
        pathType: Prefix